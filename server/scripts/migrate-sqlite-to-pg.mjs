#!/usr/bin/env node

/**
 * SQLite → PostgreSQL 增量迁移脚本（占位）
 *
 * 每个 Phase 建完新表后，在此脚本中增加对应表的数据清洗与迁移逻辑。
 *
 * 用法：
 *   node scripts/migrate-sqlite-to-pg.mjs
 *
 * 当前已实现的表迁移：
 *   - （无）
 */

console.log('⚠️ SQLite → PG 增量迁移脚本（占位）');
console.log('请在各 Phase 建完表后补充对应迁移逻辑。');
process.exit(0);
